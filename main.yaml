- hosts: installer
  remote_user: root
  roles:
  - installer
- hosts: ocp_workers,ocp_masters,ocp_lb
  remote_user: root
  roles:
  - cluster
- hosts: installer
  remote_user: root
  roles:
  - openshift_installer
- hosts: localhost
  tasks:
  - debug:
      msg: | 
        Now ssh to the installer and run the last playbook `ssh root@{{installer.public_ip}} -i {{playbook_dir}}/installer_files/id_rsa ansible-playbook /opt/installer/main.yaml`